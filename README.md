# IL-KAN-code
This repository contains the implementation of a custom deep learning model (IL-KAN) for optimizing flood forecasting model parameters. It optimizes hydrological parameters through an incremental learning mechanism combined with Kolmogorov Arnold Networks.
